import { Button, CircularProgress, Rating, Typography } from "@mui/material";

//for some reason MUI props autogeneration requires reexporting components
export const Button_ = Button;
export const CircularProgress_ = CircularProgress;
export const Rating_ = Rating;
export const Typography_ = Typography;

console.log("loaded");
